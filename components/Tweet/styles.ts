import { StyleSheet } from "react-native";
import { Direction } from "react-native-gesture-handler/lib/typescript/web/constants";

const styles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    padding: 15,
    borderBottomWidth: 0.5,
    borderColor: 'grey'
  }
});
export default styles;
